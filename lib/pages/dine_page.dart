import 'package:flutter/material.dart';

class DinePage extends StatelessWidget {
  static const routedName = '/settings';
  const DinePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
