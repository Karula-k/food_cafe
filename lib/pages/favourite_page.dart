import 'package:flutter/material.dart';

class FavouritePage extends StatelessWidget {
  static const routedName = '/settings';
  const FavouritePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
